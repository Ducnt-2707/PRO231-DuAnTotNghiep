name: .NET CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-framework:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET SDK 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      # 🔹 Restore toàn bộ solution
      - name: Restore NuGet packages
        run: dotnet restore PRO231-DuAnTotNghiep.sln

      # 🔹 Build app chính
      - name: Build main project
        run: dotnet build PRO231-DuAnTotNghiep.sln --configuration Release

  test-dotnet:
    runs-on: windows-latest
    needs: build-framework
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET SDK 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      # ✅ Restore toàn bộ để có dependency giữa project chính & test
      - name: Restore all NuGet packages
        run: dotnet restore PRO231-DuAnTotNghiep.sln

      # ✅ Build toàn solution (để .exe có sẵn)
      - name: Build entire solution
        run: dotnet build PRO231-DuAnTotNghiep.sln --configuration Release

      # 🧪 Chạy test project
      - name: Run unit tests
        run: dotnet test DuAnTotNghiep.Test/DuAnTotNghiep.Test.csproj --configuration Release --no-build --logger "trx;LogFileName=test_results.trx"

      # (Tùy chọn) Lưu kết quả test để xem trực tiếp trong GitHub
      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: NUnit-test-results
          path: '**/test_results.trx'
